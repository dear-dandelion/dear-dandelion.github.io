# 极简叙事型个人网站设计风格手册

> **使用引导(30 秒)**
> 本手册提炼自一个已完成并上线的个人网站项目,沉淀为可复用的风格规范。
> - **什么时候用**:新做一个"极简、有叙事感"的个人作品站/落地页时,直接套用本手册
> - **怎么用**:先读「一、设计风格特征」确认风格匹配 → 复制「二、设计令牌」做底色 → 按「三~七」的规范搭建 → 最后按「八、复用流程」推进项目
> - **关键原则**:设计令牌和验收标准(六、七)是硬性要求;组件与动效是惯例,可按需增删
> - **一句话记忆**:极简黑白、排版驱动、叙事同构、动效克制。

---

## 一、设计风格特征(DNA)

**1. 极简黑白,零装饰**
- 白底 + 黑字 + 灰阶,无彩色
- 无圆角堆砌、无阴影、无渐变、无分割线 —— 装饰物全部删光,靠留白撑高级感
- 例外必须有意为之且全局唯一(如:1px 浅灰照片边框、8px 媒体卡圆角)

**2. 排版即层级**
- 信息层级全靠**字号 + 字重 + 颜色**表达,不用盒子/卡片
- 标题 Thin(100)超细字重,正文常规,次级信息灰,装饰线最浅灰
- 标题都是一句观点,不是栏目名("我不相信…"而不是"关于我")

**3. 叙事与 UX 同构**
- 交互形式 = 叙事本身(如:横向滚动对应"跨界/旅程"主题)
- 文案走"不安→行动"结构:焦虑藏在行动里,不直说

**4. 数字驱动可信度**
- 具体数字代替形容词;数字用等宽字形,成组出现

**5. 动效只给内容,不给装饰**
- 入场 fade、状态呼吸、hover 揭示数据
- 装饰线、虚线、背景永不动画

**6. 诚实呈现**
- 展示进行中的作品,状态用文字角标 + 呼吸圆点,不编造进度

## 二、设计令牌(可直接复用)

```css
:root {
  /* 色彩 — 对比度均达标 WCAG AA */
  --bg: #FFFFFF;          /* 背景 */
  --ink: #111111;         /* 主文本 16.7:1 */
  --ink-2: #757575;       /* 次级文本 4.6:1,AA 下限 */
  --ink-3: #BBBBBB;       /* 仅装饰线,禁用于正文 */

  /* 字体 — 中文禁 web 字体(单字重 4-8MB) */
  --font: "PingFang SC", "HarmonyOS Sans SC", "Microsoft YaHei",
          "Noto Sans SC", system-ui, sans-serif;
  --font-latin: "Inter", var(--font);   /* 数字/英文可走 20KB 子集 */

  /* 字号 — 全站标题统一这一个值 */
  --display: clamp(1.5rem, 4.5vw, 2.75rem);  /* 站标题,Thin */
  --h2: clamp(1.75rem, 4vw, 2.5rem);
  --h3: 1.25rem;
  --body: clamp(1rem, 1.4vw, 1.125rem);      /* 行高 1.75 */
  --small: 0.875rem;

  /* 间距 — 8px 基准 */
  --s1: 8px; --s2: 16px; --s3: 24px; --s4: 40px; --s5: 64px; --s6: 96px;

  /* 布局 */
  --pad-x: clamp(24px, 6vw, 96px);   /* 站内宽留白 */

  /* 动效 */
  --ease: cubic-bezier(0.22, 1, 0.36, 1);
  --dur: 0.6s;
}
```

## 三、排版规范

| 元素 | 规格 |
|---|---|
| 站标题 | Thin 100,`--display`,桌面单行不换行 |
| 正文 | 400,行高 1.75 |
| 关键词 | 700 加粗 |
| 次级信息 | `--ink-2`,小字 |
| 数字 | 等宽数字字形,Thin 大号 |
| 中文标点 | 全角统一(半角逗号是常见败笔) |

## 四、组件惯例

- **媒体卡**:圆角 8px,无阴影;图片全懒加载、压缩到 ≤300KB
- **状态角标**:`●` 呼吸圆点 + 文字,不用纯颜色表达状态
- **数据点**:大号数字 + 小标签,成组横排或竖排
- **照片交互**:hover 浮出数据(地点/参数),"数据揭示"是高级感的来源
- **浏览器 mockup**:纯 CSS(圆点 + 地址栏)替代裸截图
- **交互签名**:一个贯穿全站的独有交互,让人记住这个站

## 五、动效规范

| 动效 | 参数 |
|---|---|
| 入场 reveal | 0.6s,translateY(16px) → 0,进入视口触发 |
| 交错 | 120ms 间隔 |
| 呼吸 | 2s,透明度 0.35↔1 |
| 交互反馈 | 0.3s |
| reduced-motion | 全部关闭 |

## 六、响应式与无障碍(硬性标准)

- **轨道类布局**:桌面横向,<768px 降级竖向文档流
- 滚轮 = **逐站翻页**,禁用 JS 平移模拟(scroll-snap 会吸回);横向触控板走原生
- 焦点 `:focus-visible` 2px 黑描边;键盘全程可走通;`aria-current` 标记当前
- 状态不只靠颜色;对比度过 AA;200% 缩放下无溢出

## 七、性能预算(硬性)

- 图片总量 <1.5MB、单图 ≤300KB、视频 ≤10MB(720p)
- 零第三方依赖、零外部字体(中文)、零追踪
- 首屏 <500KB,Lighthouse 四项 ≥95

## 八、复用流程(新项目照此走)

1. **定位**:一句话回答"这个站是谁";提炼一条叙事主线
2. **文案定稿**:全部文案先定稿,再动代码
3. **规格先行**:先写设计文档(令牌/结构/组件/验收),代码只是执行
4. **分模块实现**:骨架 → 样式 → 交互 → 响应式,每块浏览器实测
5. **验收清单先行**:先定义"怎么算做完",再写代码
6. **真实浏览器测试**:headless 回归(滚轮/键盘/降级),不测就是赌
